{
  "name": "@levity/observability",
  "version": "1.0.0",
  "private": true,
  "description": "Logger factory, application errors, schema validation and process lifecycle.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "node --test --require ts-node/register src/validate-schema.test.ts"
  },
  "dependencies": {
    "pino": "^10.3.1",
    "pino-pretty": "^13.1.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
