{
  "name": "@levity/config",
  "version": "1.0.0",
  "private": true,
  "description": "Environment validation.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "dotenv": "^17.4.0",
    "zod": "^4.3.6"
  }
}
